#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main(int argc, char *argv[]) {
	int divisao = 10, divisor = 2;
	int resultado = divisao/divisor;
	printf("Resultado %d", resultado);
	
	
	return 0;
}
